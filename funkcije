dockerBuild () {
        docker build -t rnms:0.3 . && docker run -dit --name rnms -p 8080:80/tcp -p 2055:2055/udp rnms:0.3 && docker ps
}

dockerDestroy () {
        docker stop rnms && docker rm rnms && docker rmi rnms:0.3
}

rnms () {
docker exec -i rnms $@
}
